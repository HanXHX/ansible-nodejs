---

- hosts: all
  vars:
    test_dir: /tmp/node
  pre_tasks:
    - debug: var=nodejs_upstream
  roles:
    - ../../
  post_tasks:
    - file: path={{ test_dir }} state=directory
    - npm: >
        name=jsdom
        production=yes
        path={{ test_dir }}
