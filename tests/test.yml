---

- hosts: all
  vars:
    test_dir: /tmp/node
  roles:
    - ../../
  post_tasks:
    - file: path={{ test_dir }} state=directory
    - npm: >
        name=jsdom
        production=yes
        path={{ test_dir }}
      changed_when: false
