---

- name: APT | Update
  apt: >
    update_cache=yes
    cache_valid_time=3600

- name: INCLUDE | Install Upstream repository
  include: upstream.yml
  when: nodejs_upstream

- name: APT | Install packages
  apt: pkg=nodejs state=present
